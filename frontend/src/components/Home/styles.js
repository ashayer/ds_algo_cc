import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  startButton: {
    backgroundColor: "#cf3c02",
    color: "#fff",
    "&:hover": {
      backgroundColor: "#fff",
      color: "#3c52b2",
    },
  },
}));
